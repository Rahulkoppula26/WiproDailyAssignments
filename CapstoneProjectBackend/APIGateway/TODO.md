# TODO
- [ ] Add Spring CORS configuration (allow origin http://127.0.0.1:5500, methods incl. OPTIONS, required headers) and wire it into Spring Security.
- [ ] Ensure security config permits OPTIONS/preflight for auth endpoints.
- [ ] Run backend and verify preflight + signup POST from browser works.

